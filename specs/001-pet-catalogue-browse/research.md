# Research Notes

- Use Spring Data JPA for repository access.
- Use PostgreSQL for runtime persistence and H2 only for isolated tests.
- Use a single React page for the gallery and CRUD interactions.
- Prefer MUI components for dialogs, controls, and cards, then layer custom styling on top.
- Keep filters server-backed where possible, with the frontend responsible for the query composition.
